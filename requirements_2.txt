# =============================================================================
# ASML Micro-Learning Pipeline — Python dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# Note: llama-cpp-python[server] is installed separately in the Dockerfile
# because it needs special CMAKE_ARGS for GPU builds.
# =============================================================================

# --- ASR (Whisper) -----------------------------------------------------------
faster-whisper>=1.0.0

# --- Transcript cleaning & evaluation ----------------------------------------
jiwer>=3.0.0

# --- Semantic segmentation (embeddings) --------------------------------------
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# --- Vector storage (local fallback) -----------------------------------------
numpy>=1.24.0

# --- Utilities ---------------------------------------------------------------
tqdm>=4.60.0
